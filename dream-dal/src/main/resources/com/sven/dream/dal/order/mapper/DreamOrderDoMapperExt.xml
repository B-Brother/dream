<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sven.dream.dal.order.mapper.DreamOrderDoMapperExt" >
   
  <!-- biz sql -->
  <select id="getOrderByOrderId" parameterType="java.lang.Long" resultMap="BaseResultMap"> 
	    select 
	    <include refid="Base_Column_List" />
	    from dream_order
	    where
	    order_id = #{orderId}
	    	and
	    is_deleted = 'n'	
   </select>
   
   <select id="getOrderListByQuery" parameterType="com.sven.dream.common.query.OrderQuery" resultMap="BaseResultMap"> 
	    select 
	    <include refid="Base_Column_List" />
	    from dream_order
	    where
	    seller_id = #{sellerId}
	    	and
	    pay_status = #{payStatus}	
	    	and
	    is_deleted = 'n'	
	    limit ${startIndex},${pageSize}
   </select>
   
   <select id="countOrderListByQuery" parameterType="com.sven.dream.common.query.OrderQuery" resultType="java.lang.Integer"> 
	    select 
	    count(1)
	    from dream_order
	    where
	    seller_id = #{sellerId}
	    	and
	    pay_status = #{payStatus}	
	    	and
	    is_deleted = 'n'	 
   </select>
</mapper>



